# 三消游戏开发任务计划

## 一、玩法规则基线

- 棋盘包含 普通元素、箱子、空位、火箭、炸弹、螺旋桨。
- 箱子是 静态目标：
    - 不会移动。
    - 不可交换。
    - 不参与普通三消匹配。
- 箱子只会被以下方式消除：
    - 被普通三消形成的命中格 上下左右相邻 命中。
    - 被火箭路径产生的命中格 上下左右相邻 命中。
    - 位于炸弹 5x5 覆盖范围内，被炸弹直接清除。
    - 被螺旋桨选中后直接清除。
- 箱子被清除后，其所在格子变为 普通空位，后续允许参与掉落和补位。
- 补位规则：
    - 优先竖直下落。
    - 若不能竖直补位，允许从左上或右上斜向掉落。
    - 若箱子下方或局部形成多个连续空位，左右来源按随机策略填充。
- 火箭生成规则：
    - 横向四连 生成 纵向消除火箭。
    - 纵向四连 生成 横向消除火箭。
- 炸弹生成规则：
- T/L 形五连 生成炸弹。
- 炸弹只清除以自身为中心的 5x5 范围。
- 5x5 外的箱子绝不参与本次炸弹消除。
- 螺旋桨生成规则：
    - 2x2 方块匹配生成螺旋桨。
    - 正常触发时，在普通消除完成后，从 当前最上层仍存在的箱子 中随机选一个并清除。
- 火箭与螺旋桨相邻交换的特殊规则：
    - 先执行螺旋桨的普通消除逻辑。
    - 不执行螺旋桨“选箱消除”逻辑。
    - 暂不执行当前交换火箭的单体逻辑。
    - 然后在棋盘上寻找一个 方向不同 的现有火箭。
    - 若找到，则在该火箭位置执行 双火箭十字消除，螺旋桨消失。
    - 若找不到，则在交换时直接执行当前火箭逻辑。

## 二、开发任务拆解

> ### 任务 1：建立棋盘与格子数据模型  
> - 目标：定义格子类型、坐标系统、棋盘尺寸、占位规则。  
> - 验收方式：能用数据表达普通元素、箱子、空位和三种道具。

> ### 任务 2：实现基础渲染  
> - 目标：把棋盘、普通元素、箱子、火箭、炸弹、螺旋桨正确显示出来。  
> - 验收方式：运行后可清晰看到完整棋盘和不同对象类型。  

> ### 任务 3：实现初始盘面配置  
> - 目标：支持配置初始元素布局、箱子坐标和目标总数。  
> - 验收方式：同一配置多次启动时，盘面一致。  

> ### 任务 4：实现交换输入  
> - 目标：支持拖动或点击交换相邻普通元素/道具。  
> - 约束：箱子不可交换、不可拖动。  
> - 验收方式：普通元素与道具可交换，涉及箱子的交换无效。  

> ### 任务 5：实现匹配检测与回退  
> - 目标：识别横纵 3+ 连线；未形成消除则回退。  
> - 实现假设：暂假设若无可消除组合该交换视为无效，元素回退至交换前状态。
> - 验收方式：有效交换进入结算，无效交换回退。  

> ### 任务 6：实现普通三消结算  
> - 目标：移除匹配到的普通元素，并生成本轮“命中格集合”。  
> - 验收方式：普通元素正确消除，后续逻辑可复用命中格数据。  

> ### 任务 7：实现箱子相邻受击  
> - 目标：让与普通消除格 上下左右相邻 的箱子被清除。  
> - 验收方式：仅正交相邻箱子受击，斜对角不受影响。  

> ### 任务 8：实现重力、斜向掉落与补位  
> - 目标：消除后空位可由上方或左右上方元素填充，并从顶部补新元素。  
> - 验收方式：普通元素能竖直或斜向下落，箱子清除后的空位可被填满。  

> ### 任务 9：实现连锁状态机  
> - 目标：将一轮完整操作串成闭环：  
> - 交换 -> 匹配 -> 普通消除 -> 箱子受击 -> 下落/补位 -> 再检测  
> - 验收方式：一次交换可触发多轮连锁，稳定前禁止玩家再次输入。  

> ### 任务 10：实现道具生成  
> - 目标：根据匹配形状生成火箭、炸弹、螺旋桨。  
> - 验收方式：  
> 
> 横向四连生成纵向火箭。  
> 纵向四连生成横向火箭。  
> T/L 五连生成炸弹。  
> 2x2 方块生成螺旋桨。  

> ### 任务 11：实现火箭单体效果  
> - 目标：火箭清一整行或一整列。  
> - 约束：火箭路径命中格不触发箱子相邻受击。  
> - 验收方式：火箭能清线并推进清箱。  

> ### 任务 12：实现炸弹单体效果  
> - 目标：炸弹只清除自身中心 5x5 范围。  
> - 约束：范围外箱子绝不参与此次消除。  
> - 验收方式：炸弹爆炸后，只有 5x5 内内容变化。  

> ### 任务 13：实现螺旋桨单体效果  
> - 目标：螺旋桨完成自身十字消除后，再锁定最上层箱子中的一个随机清除。  
> - 验收方式：消除以螺旋桨为中心的十字后，会飞向最上层箱子之一并完成清除。  

> ### 任务 14：实现火箭+螺旋桨相邻交换特殊逻辑  
> - 目标：支持这组特殊交换效果。  
> - 验收方式：  
>
> 找到异向火箭时，在该火箭位置触发十字双火箭消除。  
> 找不到异向火箭时，退化为执行当前火箭逻辑。  

> ### 任务 15：实现顶部目标栏  
> - 目标：显示剩余箱子数量。  
> - 验收方式：箱子每被真正清除一个，目标数实时减少 1。  

> ### 任务 16：实现胜利条件与结算  
> - 目标：箱子数归零后停止交互，显示胜利反馈。  
> - 验收方式：最后一个箱子清除后进入 Great 或同等结算表现。  

> ### 任务 17：实现随机性控制与演示稳定性  
> - 目标：通过固定初始盘面和统一随机种子，让 Demo 稳定可复现。  
> - 验收方式：同一种子下运行结果一致，不同种子下玩法仍正确。  

## 三、重点边界条件

1. 箱子不参与交换，也不参与普通匹配判定。  
2. 箱子被清除后，原格必须立刻变为空位，而不是保留障碍状态。  
3. 普通三消的“相邻受击”只认 上下左右，不认斜对角。  
4. 若同一个箱子同时被多个命中格邻接，仍只应被清除一次。  
5. 火箭路径不会触发箱子相邻受击，炸弹不做范围外扩散。  
6. 炸弹只结算 5x5 内对象，5x5 外箱子即使贴边也不能被消除。  
7. 螺旋桨正常触发时，必须在“当前最上层箱子集合”中随机选目标，而不是全局随8.机。  
8. 若最上层有多个箱子，随机选择应可复现，建议挂到统一随机种子。  
9. 火箭+螺旋桨交换时，螺旋桨不能再执行“选箱消除”。  
10. 火箭+螺旋桨交换时，如果棋盘上没有异向火箭，必须稳定退化为当前火箭逻辑，不能无事发生。  
11. 双火箭十字消除的触发位置是“后找到的那枚异向火箭位置”，不是交换位置。  
12. 斜向掉落时，如果左上和右上都可填充，需有明确选择策略，建议统一走随机种子。  
13. 连锁过程中必须锁输入，避免玩家在棋盘未稳定时再次交换。  
14. 补新元素后如果天然形成新三消，应自动继续结算。  
15. 目标栏只在箱子真正移除时减少，不能因为命中检测或动画提前扣数。  
16. 最后一个箱子被清除时，要先完成当前必要结算，再进入胜利，避免动画和状态截断。  